apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "apihut.fullname" . }}-test-connection"
  labels:
    {{- include "apihut.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "apihut.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
