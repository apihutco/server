apiVersion: v1
kind: Service
metadata:
  name: {{ include "apihut.fullname" . }}
  labels:
    {{- include "apihut.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "apihut.selectorLabels" . | nindent 4 }}
